
import Auth from '../Shared/Auth';
import Reset from '../Components/Password/Reset';

const ResetPassword = () => {
    return (
        <Auth>
            <Reset/>
        </Auth>
    );
};

export default ResetPassword;